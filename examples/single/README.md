# Single private cluster

Use `config/global.tfvars` followed by `config/uks/dev/dev.tfvars` at the repository root. This is the complete single-cluster example; it references an existing network and uses AKS-managed private DNS and managed load-balancer outbound. Replace synthetic IDs and review cost/capacity before an authenticated plan. See [getting started](../../docs/getting-started.md).
